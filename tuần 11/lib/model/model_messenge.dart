class ModelMessenge {
  String isMe, messageType, message, profileImg, id;
  ModelMessenge(
      {
        required this.id,
      required this.isMe,
      required this.messageType,
      required this.message,
      required this.profileImg});
}
